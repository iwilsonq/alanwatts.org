import React, { Component } from 'react';
import Hero from './hero';

export default class Home extends Component {
  render() {
    return (
      <div className="home">
        <Hero />
      </div>
    );
  }
}
